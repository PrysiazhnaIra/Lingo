import { NavLink } from "react-router-dom";
import Logo from "../Logo/Logo.jsx";

export default function Header() {
  return (
    <header>
      <Logo />
      {/* <NavLink></NavLink> */}
    </header>
  );
}
